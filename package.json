{
  "name": "cursor-2025",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "live-server --port=5500 --ignore=\"**/AppData/**\"",
    "dev": "live-server --port=5500 --ignore=\"**/AppData/**\"",
    "build": "echo \"Build complete - static files ready\"",
    "serve": "node serve-mainpro.js",
    "serve-live": "live-server --port=3000",
    "serve-chrome": "live-server --port=5500 --browser=chrome --ignore=\"**/AppData/**\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "jspdf": "^3.0.3",
    "jspdf-autotable": "^5.0.2",
    "openai": "^6.4.0",
    "xlsx": "^0.18.5"
  }
}
